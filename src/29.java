public class SchoolProjectRepository {
    public void addStudent(Student student) {
        // Add the student to the repository
    }

    public List<Student> getAllStudents() {
        // Get all students from the repository and return a list of them
    }

    public boolean updateStudent(int id, Student updatedStudent) {
        // Update an existing student with the given ID in the repository
    }
}
